apiVersion: v1
kind: ConfigMap
metadata:
  name: shares-gains-app-config
  labels:
    app: shares-gains-uk-tax-calculator
data:
  NODE_ENV: "production"
  AUTH_EMAIL_PROVIDER: "noop"
  NEXT_PUBLIC_APP_URL: "https://example.com"
  NEXT_PUBLIC_BETTER_AUTH_URL: "https://example.com/api/auth"
  BETTER_AUTH_URL: "https://example.com/api/auth"
