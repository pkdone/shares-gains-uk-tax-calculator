Undiscovered Bugs. Act as a senior programmer analyzing the code in a TypeScript application where the content of each file in the application's codebase is shown below in a code block. Identify all the places where there are bugs in the code and how they should be fixed. Include the list of relative file paths of each source file affected. Before finishing, you should include the steps to run compile, lint, and unit test checks, fixing any problems.