Wrong Method Visibility: Act as a senior programmer analyzing the code in a TypeScript application where the content of each file in the application's codebase is shown below in a code block. Identify any places in the code where class methods are public/protected but should be less visible (protected/private) due to only being used by sub-classes or from internally within the same class. Also, highlight any things (including types, interfaces, functions, constants) that are exported but don't need to be exported as they are only referenced by other code in the same file. Provide references to the specific part(s) of the code that needs these improvements with suggestions on how to improve. Include the list or relative file paths of each source file affected.
