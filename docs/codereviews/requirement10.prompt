Code Duplication: Act as a senior programmer analyzing the code in a TypeScript application where the content of each file in the application's codebase is shown below in a code block. Identify the top 10 places in the code where very similar logic or code is being used as other parts of code where it is obvious the similar code could be factored out into a single place and shared. Provide references to the specific part(s) of the code that needs these improvements with suggestions on how to improve. Include the list or relative file paths of each source file affected.