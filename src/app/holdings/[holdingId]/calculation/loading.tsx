/**
 * Route-level loading UI during client navigation to `/calculation`.
 * Kept blank so the capital gains view appears when ready without interim copy.
 */
export default function HoldingCalculationLoading(): null {
  return null;
}
