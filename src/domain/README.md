# Domain layer

Entities, value objects, aggregates, domain services, repository **interfaces**, and canonical Zod schemas (`schemas/`). No framework or database imports.

See `.cursor/rules/project.mdc` and `docs/adrs/001-folder-structure-and-ddd-layering.md`.
