/**
 * Re-export for call sites that import Better Auth collection names from this module.
 * Canonical definitions live in {@link collection-names}.
 */
export { BETTER_AUTH_COLLECTION_NAMES } from '@/infrastructure/persistence/collection-names';
