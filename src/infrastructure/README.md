# Infrastructure layer

Repository implementations, MongoDB persistence, schema registry (from Milestone 2 onward), and external adapters. Implements ports defined in `src/domain/repositories/`.

See `.cursor/rules/project.mdc`.
