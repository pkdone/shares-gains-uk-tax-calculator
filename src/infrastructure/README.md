# Infrastructure layer

Repository implementations, MongoDB persistence, schema registry, broker import adapters (e.g. E\*Trade XLSX/PDF), and external integrations. Implements ports defined in `src/domain/repositories/`.

See `.cursor/rules/project.mdc`.
